@startuml Activity Diagram - Transaksi
skinparam activityFontSize 11
skinparam activityDiamondFontSize 10

|Pekerja Lepas|
start
:Membuka menu transaksi;
:Memilih jenis transaksi;
note right
  Jenis Transaksi:
  - Tagihan
  - Penerimaan
  - Pengeluaran
end note
:Mengisi form transaksi;
:Klik tombol simpan;

|Sistem|
:Menerima data transaksi;
:Validasi data;

if (Data valid?) then (tidak)
  :Menampilkan pesan error;
  
  |Pekerja Lepas|
  :Melihat pesan error;
  stop
else (ya)
  |Sistem|
  :Menyimpan transaksi ke database;
  :Generate 2 jurnal entries (Debit dan Credit);
  :Menyimpan jurnal ke database;
  :Menampilkan pesan sukses;
  
  |Pekerja Lepas|
  :Melihat konfirmasi sukses;
  stop
endif
@enduml
