@startuml Deployment Diagram

' ==================== DEVELOPER ENVIRONMENT ====================
node "Developer Device" as devDevice <<development>> {
  artifact "Source Code" as src {
    component "src/main/java"
    component "pom.xml"
  }
  
  component "Maven" as maven
  component "JDK 23" as jdk
  
  artifact "freenance-1.0.jar" as buildJar <<build output>>
}

src --> maven : <<build>>
maven --> jdk : <<uses>>
maven --> buildJar : <<produces>>

note right of devDevice
  **Developer Tools:**
  - JDK 23 (not JRE)
  - Maven
  - IDE (optional)
  
  **Build Command:**
  mvn clean package
  
  **Output:**
  target/freenance-1.0.jar
end note

' ==================== DEPLOYMENT ====================
buildJar ..> "Distribution" : <<deploy>>

' ==================== END USER ENVIRONMENT ====================
node "End User Device" as userDevice <<execution environment>> {
  artifact "freenance.exe" as exe <<Windows>> {
    note right
      Windows executable wrapper
      (launch4j/jpackage)
    end note
  }
  
  artifact "freenance.sh" as sh <<Linux/macOS>> {
    note right
      Shell script wrapper:
      #!/bin/bash
      java -jar freenance-1.0.jar
    end note
  }
  
  artifact "freenance-1.0.jar" as jar <<cross-platform>> {
    note right
      Executable JAR berisi:
      - Compiled classes
      - All dependencies embedded:
        * sqlite-jdbc 3.49.1.0
        * jasperreports 7.0.3
        * jfreechart 1.5.5
        * jcalendar 1.4
        * jbcrypt 0.4
        * jackson-databind 2.19.0
        * dotenv-java 3.2.0
    end note
  }
  
  database "SQLite Database File" as db <<auto-generated>> {
    artifact "freenance.db"
  }
  
  component "Java Runtime Environment" as jre <<runtime>> {
    artifact "JRE 23"
  }
}

exe --> jar : <<wraps>>
sh --> jar : <<wraps>>
jar --> jre : <<runs on>>
jar --> db : <<creates & connects>>

note right of userDevice
  **Platform:**
  - Windows
  - Linux
  - macOS
  
  **Requirements:**
  - JRE 23 or higher
  - Minimum 2GB RAM
  - 100MB disk space
  
  **Files Distributed:**
  - Windows: freenance.exe + freenance-1.0.jar
  - Linux/macOS: freenance.sh + freenance-1.0.jar
end note

note bottom of sh
  **End User Experience:**
  
  **Windows:**
  Double-click freenance.exe
  
  **Linux/macOS:**
  Double-click freenance.sh
  (atau: ./freenance.sh di terminal)
  
  **Semua platform: No manual command!**
end note

@enduml
