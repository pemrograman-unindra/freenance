@startuml Activity Diagram - CRUD Data Master
skinparam activityFontSize 11
skinparam activityDiamondFontSize 10

|Pekerja Lepas|
start
:Membuka menu data master;
:Memilih sub menu data master;
note right
  - Kategori Keuangan
  - Data Kontak
  - Data Proyek
  - Merencanaan Anggaran
end note
:Memilih aksi;
note right
  - Lihat
  - Tambah
  - Edit
  - Hapus
end note

if (Aksi Lihat/Cari?) then (tidak)
  if (Aksi Hapus?) then (tidak)
    :Mengisi/mengubah data;
    :Klik tombol simpan;
    
    |Sistem|
    :Menerima data;
    :Validasi data;
    
    if (Data valid?) then (tidak)
      :Menampilkan pesan error;
      
      |Pekerja Lepas|
      :Melihat pesan error;
      stop
    else (ya)
      :Simpan/update database;
      :Menampilkan pesan sukses;
      
      |Pekerja Lepas|
      :Melihat konfirmasi sukses;
      stop
    endif
    
  else (ya)
    :Memilih data dari list;
    :Klik tombol hapus;
    :Konfirmasi penghapusan;
    
    if (Konfirmasi ya?) then (tidak)
      :Batal hapus;
      stop
    else (ya)
      |Sistem|
      :Menghapus dari database;
      :Menampilkan pesan sukses;
      
      |Pekerja Lepas|
      :Melihat konfirmasi sukses;
      stop
    endif
  endif
  
else (ya)
  :Mengisi keyword pencarian
  (opsional);
  :Klik tombol cari;
  
  |Sistem|
  :Query data dari database;
  :Filter sesuai keyword;
  :Menampilkan daftar data;
  
  |Pekerja Lepas|
  :Melihat daftar data;
  stop
endif

@enduml
